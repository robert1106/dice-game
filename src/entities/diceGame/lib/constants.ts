export enum POSITION_VARIANTS {
  under = 'Under',
  over = 'Over',
}

export enum RESULT_COLUMNS_TYPES {
  time = 'time',
  guess = 'guess',
  result = 'result',
}

export enum STATUS_TYPE {
  won = 'won',
  lost = 'lost',
}
